1.
Напишите метод, который, при наличии двух аргументов 
width и height, выводит в консоль текст, 
представляющий прямоугольник с указанными размерами.

Прямоугольник должен быть заполнен пробелами, 
а его границы должны состоять из звездочек (*).

Например, даны width = 3 и height = 3:

***
* *
***

Заканчивайте каждую строку (включая последнюю) 
комбинацией символов возврата каретки и перевода 
строки. ("\n")

Примечание: вы можете предположить, что ширина 
и высота всегда будут больше нуля.

НЕ ИСПОЛЬЗУЙТЕ INPUT()

function(3, 3)



2.
Число является палиндромом, если оно равно числу, 
состоящему из цифр в обратном порядке. 
Например, 5, 44, 171, 4884 являются палиндромами, 
а 43, 194, 4773 — нет.

Напишите функцию, которая принимает положительное 
целое число и возвращает количество специальных 
действий, необходимых для получения палиндрома. 
Специальный шаг: «перевернуть цифры и сложить с 
исходным числом». Если полученное число не является 
палиндромом, повторяйте процедуру с суммой, пока 
полученное число не станет палиндромом.

Если введенное число уже является палиндромом, 
количество шагов равно 0.

Все входные данные гарантированно имеют конечный 
палиндром.

Пример
Например, начните с 87:
  87 +   78 =  165     - step 1, not a palindrome
 165 +  561 =  726     - step 2, not a palindrome
 726 +  627 = 1353     - step 3, not a palindrome
1353 + 3531 = 4884     - step 4, palindrome!

4884 является палиндромом, и нам нужно было
сделать 4 шага для его получения, поэтому 
ответ 87 на 4.

3. Строка матрицы с максимальной суммой

Дана матрица n x m (список списков). 
Найти номер строки с максимальной суммой 
элементов и значение этой суммы.
вернуть 

4. Сложение матриц

Даны матрица N x M. 

5. Умножение матриц

Написать функцию, которая принимает две матрицы.
Возвращает матрицу-результат.

Правила
Матрицы A и B могут быть перемножены, если число 
столбцов матрицы A равно числу строк матрицы B.  

Каждый элемент результирующей матрицы вычисляется 
как сумма произведений соответствующих элементов 
строки матрицы A и столбца матрицы B. То есть 
элемент C(i, j) равен сумме произведений элементов 
i-й строки A на элементы j-го столбца B. 

Результат произведения имеет столько же строк, 
как первая матрица, и столько же столбцов, как 
вторая матрица.
Если A имеет размерность m × n, а B — n × p, то 
их произведение будет матрицей размером m × p.


6. Десятичные дроби или группы тысяч?
В таких странах, как США и Китай, для обозначения
десятичной точки используется точка, а для разделения 
тысяч — запятая, например, 4,353.56.

В других странах (в основном в Европе) для обозначения
десятичной точки используется запятая, а также точка
для разделения тысяч, например, 4.353,56.

Ваша задача — суммировать массив строковых 
представлений чисел в одном из двух указанных 
выше форматов с точностью не более двух десятичных 
знаков. Полученная сумма должна представлять собой 
строку в формате xx,xxx.xx (сохраняйте два десятичных 
знака, разделяйте группы тысяч запятой при необходимости).

Ввод - ["1,234.56", "10.000,50", "999,999.99", "99,999", "0.005"]

7. Сумма диагоналей

Написать функцию, которая будет принимать 
в себя квадратную матрицу, то есть 
количество столбцов и строк которой 
будут равны. Вернуть сумму всех элементов 
в прямой и обратно диагоналях.

8. Крестики нолики
solve/check_game_end

Написать функцию, которая проверяет:
произошел ли конец игры, и 
говорит результат. 

Функция получает матрицу с ходами.
Пустая ячейка - (-1)
О - (0)
Х - (1)

Результатом может 
быть победа О, победа Х, ничья.
Игра не окончена - (-2)
Ничья - (-1)
Победа О - (0)
Победа Х - (1)


